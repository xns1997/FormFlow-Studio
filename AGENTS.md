# Repository agent instructions

Codex 必须先阅读并遵守根目录 `CODEX.md`。其中定义了 FormFlow MCP/HTTP 工具发现、revision、幂等、破坏性确认、数据边界和发布门禁。

处理 `.formflow` 项目包时还必须遵守 `.codex/skills/formflow-project-editor/SKILL.md`；优先使用统一 MCP 工具，离线包编辑才使用该 skill 的 CLI。
