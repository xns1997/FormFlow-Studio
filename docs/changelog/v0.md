# 0.x

## [0.9.0] - 2026-07-13

### 新增

- 新增仓库内置 skill [`../../.codex/skills/formflow-project-editor/`](../../.codex/skills/formflow-project-editor/)，用于让智能体以紧凑 YAML 创建、编辑、校验和打包 FormFlow v2 项目
- 新增确定性 CLI [`../../.codex/skills/formflow-project-editor/scripts/formflow-project.mjs`](../../.codex/skills/formflow-project-editor/scripts/formflow-project.mjs)，支持 `inspect`、`create`、`normalize`、`validate`、`pack` 和 `unpack`
- 新增冻结的 FormFlow v2 引用文档，覆盖作者输入格式、项目结构、规范化规则、引用完整性和节点端口校验
- 新增基于 skill 生成的示例项目 `projects/skill-demo`

### 校验与兼容性

- `create` / `normalize` 统一输出规范化目录结构、稳定排序和一致的 JSON 缩进，重复执行结果可复现
- 数据导入支持 Excel、CSV 和 JSON，并在生成阶段校验空 key、重复 key、字段推断和显示配置
- `validate` 增加对 schema、稳定 ID、跨文件引用、流程端口连接和项目交付门禁的机器可读结果输出
- 遇到未知字段或插件扩展字段时立即报错并列出路径，避免静默丢失信息

### 修复

- 修复通过 skill 生成项目时对 UTF-8 中文 CSV 的解析问题，确保示例数据和导入场景可稳定通过校验

## [0.8.0] - 2026-07-11

### 流程引擎核心改进

#### 新增执行选项

- **`onNodeFailure`**: 节点失败策略 — `'abort'`(默认，停止流程)、`'skip'`(跳过继续)、`'continue'`(同 skip)
- **`timeoutMs`**: 全局流程超时（毫秒），超时后立即终止流程
- **`nodeTimeoutMs`**: 单节点超时（毫秒），超时后该节点失败
- **`parallel`**: 并行执行选项，无依赖关系的节点同时执行（`Promise.all`）
- **`isolatedScopes`**: 变量作用域隔离，防止同名节点输出互相覆盖
- **`debug`**: 调试模式，debug 事件包含变量快照（输入/输出实际值）
- **`transactionalSideEffects`**: 事务性侧效果，流程失败时自动回滚已收集的侧效果

#### 新增流程控制节点

- **`generic-condition-branch`**: 条件分支节点，根据 JS 表达式路由到 true/false 分支
- **`generic-for-each`**: 遍历节点，遍历数组中的每个元素
- **`generic-call-workflow`**: 调用流程节点，配置子流程 ID 并传递输入数据

#### 服务端改进

- 新增 `/api/checkpoints` 端点（POST / GET / DELETE），支持服务端存储流程检查点
- `checkpoint` ID 参数增加 `sanitizeId` 校验，防止路径遍历攻击

### 行为引擎增强

- `callApi` 支持请求体、认证、响应回写、可配置超时和重试
- 条件求值新增 `dataSource`，可读取流程输出和其他行为结果
- 新增 `runWorkflow` 动作，并支持流程输出自动回写表单字段

### 表单-流程-行为联动改进

- 流程执行成功后，自动将 `workflow:export` 输出字段匹配并回写到表单
- `ExecuteFlowOptions` 新增 `executionOrder`，默认 `['linkage', 'script', 'flow']`
- 联动规则中的 `runWorkflow` 动作增加输出自动回写

### 移动端适配与安全修复

- 新增 `responsive.css` 响应式样式（768px / 1024px 断点 + 触摸优化 + 打印样式）
- 修复 `onNodeFailure: 'abort'` 未实际停止执行的问题
- 修复 checkpoint API 路径遍历漏洞

### 工程改进

- 节点总数从 144 增加到 **147**
- 所有新选项默认 `undefined`，保持向后兼容
- TypeScript 类型检查通过，28 / 28 单元测试通过

## [0.7.1] - 2026-07-09

### 设计器画布修复

- 修复画布初始化尺寸为 `0x0` 的问题（`requestAnimationFrame` + 重试机制）
- `syncGraphSize` 在容器尺寸为 0 时添加 50ms 重试，最多 20 次

### 文档样式优化

- 文档卡片、表格、Modal 内布局与 hover 指示器整体优化

### 新增示例项目

- `example_shanghai_catering`：覆盖 16 个区、42 个月月度数据与预测汇总

### 节点包更新

- 新增 8 个节点包，删除 6 个冗余节点

### 工程改进

- 新增自动布局、脚本运行时、表格编辑器、组件 Playground、调试抽屉与按钮系统样式

## [0.7.0] - 2026-07-08

### 工作流 I/O 与 CRUD

- 新增 `workflow:import`、`workflow:export`
- 新增 6 个 CRUD 节点与 2 个数据处理节点
- 输入端口支持多连接、覆盖值和项目数据表绑定

### 示例与工程

- 新增阀门二代、三代选型示例
- 增加跨平台初始化脚本 `scripts/init-env.sh` / `scripts/init-env.ps1`
- 节点总数从 134 增加到 **144**

## [0.6.0] - 2026-07-07

### UI 与文档系统重构

- `services/`、`pages/`、`designer/`、CSS 与行为文档数据模块全面拆分
- 新增 `/docs` 首页与 5 大文档分区
- 增加模糊搜索、标签过滤、面包屑、侧边目录和详情导航

### 右侧面板样式优化

- 全局右侧面板宽度、圆角、阴影、间距与 antd 控件尺寸整体收紧

## [0.5.0] - 2026-07-06

### 使用模式页面

- 新增独立“使用模式”页面（`/projects/:id/usage`），集成数据预览与表单预览弹窗

### 后端数据 API 改造

- 新增项目作用域数据 API，统一读写 `.formflow` 包数据

### 表单与设计器优化

- 表单字段变更可自动回写工作表
- 设计器支持表单切换、配置面板性能优化
- 新增 `generic-custom-js` 节点与 Monaco 代码编辑器集成

### 视觉与示例

- 项目列表卡片改为 Mesh Gradient 封面
- 修复多个行业示例项目写回字段映射

## [0.4.0] - 2026-07-03

### FormFlow v2 项目架构

- 项目持久化从单文件 JSON 改为 `projects/data/<id>.formflow/` 目录包
- 前后端全部切换到严格识别 `project.json`
- 前端移除旧 JSON 导入导出，ZIP 与目录包共享相同结构

### 仓库目录重构

- 前端迁入 `ui/`，后端整理为 `server/src/`，Python 能力整理为 `python-service/`
- 新增 `server/src/config/paths.ts` 统一路径管理

### Vite 8 与节点包自动发现

- 构建升级到 Vite 8.1 与 `import.meta.glob` 自动注册节点
- 节点执行器改为动态导入，生产构建生成独立 chunk

### 工程质量

- 新增节点发现测试、Playwright 依赖与 `test:e2e`
- 当前验证基线覆盖单测、E2E、类型检查、生产构建、后端冒烟测试和 Python 语法检查

## [0.3.0] - 2026-07-01

### 交互体验优化

- 新增智能行切换器、自动聚焦、键盘流、即时校验、分步向导与卡片式布局

### 行为定义系统完善

- 补全 7 个动作执行器
- 新增 14 个行为模板、可视化规则构建器、测试面板和导入导出

### 行为事件扩展

- 扩展到 24 个事件，覆盖表单、字段、行、导入导出和值变化场景

## [0.2.0] - 2026-06-30

### 全部节点执行器真实可运行

- **133 个节点**全部具备真实执行逻辑，无 stub
- 覆盖数据库查询、WebSocket、PDF 报告、邮件、样式、图表、数据校验、工作表保护等场景

### Range Selector Excel 级增强

- 增加 Name Box、Formula Bar、键盘导航、多区域选择、自动滚动、列宽调整和右键菜单

### 表单设计器预览模式与行为触发

- 预览模式下控件不可拖拽但可交互
- 表单加载、行为规则、脚本沙箱与离线兼容整体增强

### Monaco 与滚动修复

- 新增自定义浅色主题、输出端口展开弹窗、来源信息提示与多处滚动/被动事件修复

## [0.1.0] - 2026-06-29

### 初始版本

- 数据预览（AG Grid + 字段类型推断）
- 流程编排（节点画布 + 131 节点）
- 表单设计器（14 种控件 + X6 画布）
- 行为定义（脚本编辑器 + 行为引擎）
- 测试运行（表单预览 + 变更记录 + 导出）
- Monaco 代码编辑器
- Range Selector 基础功能
- Express 后端 + Python ML 后端
