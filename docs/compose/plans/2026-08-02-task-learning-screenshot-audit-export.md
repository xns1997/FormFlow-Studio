# 「按任务学习」文档步骤配图整改 Todo（导出版）

> 更新时间：2026-08-02  
> 执行原则：先改步骤文案，再按文案核图；只有文案与画面一致时，截图才允许保留。

## 当前结论

- 已先修正文案层，使任务步骤描述与现有画面一致。
- 已去掉 `测试与质量` 中“同一张测试面板图重复承担第 1 步和第 4 步”的错误步骤。
- 已把 `使用与导出` 从“使用页 + 数据页”的叙述改回当前真实画面对应的“数据页搜索 + 导出结果”链路。
- DSL / reference 入口问题已并入同轮整改，不再允许跳到不存在的 `/docs/:slug`。

## 按页 Todo

### 01 认识与创建

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 文案不符 | 原第 4 步写“进入编辑器确认工作模式”，但当前图是数据页，且能看到 4 张数据表。 | 已改文案为“创建后先进入数据页，确认示例项目已经打开并显示 4 张数据表”。 |
| 已完成 | 文案不符 | 原第 2 步把起始方式简写成“选择空白项目或内置模板”，漏掉了 `.formflow 导入`。 | 已改文案，按当前向导画面列出 3 种起始方式。 |

### 02 导入与建模

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 文案不符 | 原第 2 步泛写“核对字段类型”，但当前图明确是 `work_records.json / 工作记录` 数据表。 | 已改为指向 `work_records.json`，并要求核对表头、样本值和字段数量。 |
| 已完成 | 文案不符 | 原第 3 步只写“勾选 Key 字段”，未说明当前图里勾选的是 `工作记录ID`。 | 已改为“为 work_records.json 勾选 工作记录ID 作为 Key”。 |
| 已完成 | 错图 | 原第 4 步一度使用 `JOB-0001` 搜索图，但实际结果是 `10 / 900` 的前缀匹配，不是单条目标记录。 | 已用 Playwright 重拍为搜索 `JOB-00014` 的唯一命中结果，并把文案改成“收敛到 1 条目标记录”。 |

### 03 生成与设计表单

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 文案不符 | 原第 1 步只写“选择字段”，当前图明确勾选了 `工作记录ID` 与 `从业者ID` 两列。 | 已改成按当前画面点名这两个字段。 |
| 已完成 | 文案不符 | 原第 2 步写“推荐模板”，当前图实际高亮的是 `单表数据录入` 模板预览。 | 已改成“确认 单表数据录入 模板预览，并准备 创建并打开”。 |
| 已完成 | 连续性断裂 | 原第 4 步写“进入真实填写”，容易让人误以为这是完整业务运行页；当前图更接近“创建并打开后的运行预览”。 | 已把文案收窄为“确认表单骨架已经生成并可继续填写”。 |

### 04 行为与流程

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 文案不符 | 原第 1 步泛写“提交校验与提醒语句”，当前图能明确看到 `before submit` 必填校验与工时提醒。 | 已把规则语句写成更贴近当前画面含义的检查点。 |
| 已完成 | 文案不符 | 原第 3 步只说“字段映射与必填字段”，当前节点配置里还包含主键字段。 | 已补上“主键字段、必填字段和字段映射配置”。 |

### 05 模板应用

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 步骤重复风险 | 通用模板逻辑原本分散在多个模板页，容易让同一批截图在不同页面重复出现。 | 已保持主任务页轻量，并继续使用《通用模板使用逻辑》承接公共流程。 |

### 06 测试与质量

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 重复步骤 | 原第 1 步与第 4 步都在讲同一张“自动测试样例”总览图。 | 已删除重复的第 4 步，把页面收敛为 3 步。 |
| 已完成 | 重复截图 | `test-quality-01.png` 与 `test-quality-04.png` 对应的是同一类测试总览状态，后者不再提供新信息。 | 已停止在任务文档中使用第 4 张图；当前文档只保留前 3 张。 |
| 已完成 | 文案不符 | 原第 2 步写“点击一键运行全部”，当前保留图更适合表达“核对样例结果列表”，不是按钮动作本身。 | 已改成“在同一面板中核对必填为空、枚举校验和边界值等样例结果”。 |

### 07 使用与导出

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 错图 | 原第 1 步写“在使用页打开目标表单”，但当前图实际是 `worker_profiles.json` 数据表搜索 `W-0001`。 | 已改文案为“在 worker_profiles.json 中搜索 W-0001”。 |
| 已完成 | 文案不符 | 原第 2 步写“切到数据页”，但当前第 1、2 步都在同一数据页筛选状态里。 | 已改成“保持当前筛选结果，核对导出前的表格范围就是这 1 行记录”。 |
| 已完成 | 重复截图 | `use-export-02` 与 `use-export-03` 一度是同一筛选状态截图，只靠框选区别“核对结果”和“点击导出”，会误导读者。 | 已用 Playwright 重新生成第 3 步截图，使其明确处于“导出结果”按钮高亮状态，并新增测试禁止同页复用字节级重复截图。 |
| 已完成 | 文案不符 | 原第 4 步强调“下载文件为 1 行 XLSX”，当前图实际展示的是右下角成功提示。 | 已改成“确认右下角提示 已导出当前结果（1 行）”。 |

### 08 打包与发布

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 文案校准 | 原步骤已基本对图，但已与同轮测试样例/发布门禁 wording 做统一。 | 保留 4 步结构，仅统一措辞，不再额外夸大“后续只需”。 |
| 已完成 | 文案不符 | 原第 2 步写“失败用例与发布阻断项一一对应”，但当前截图可直接证明的是“失败列表里已出现正常填写、订单数最小边界、工时边界等阻断项”，并不能一次性证明完整一一对应。 | 已把文案收窄到截图能直接证明的失败项列表。 |
| 已完成 | 文案不符 | 原第 4 步把“清掉自动测试阻断再发布”写成既定后续动作，超出了当前“已保存”截图本身可证明的范围。 | 已改为“确认右上角显示已保存，且右侧配置摘要保持最新状态”。 |
| 已完成 | 连续性断裂 | 第 2 步一度沿用了别页测试样例弹窗状态，虽然内容相似，但它不是从本页发布链路直接截取。 | 已用 Playwright 在同一条 `打包与发布` 链路中重拍第 1、2 步，统一时间、项目和测试状态。 |

## DSL / Reference 入口整改

| 状态 | 问题类型 | 当前问题证据 | 修正动作 |
| --- | --- | --- | --- |
| 已完成 | 错路由 | 旧 `buildDocsPath(slug)` 会拼出 `/docs/:slug`，而该路由并不存在。 | 已改为解析到 canonical reference path；未知 slug 回退到搜索页。 |
| 已完成 | Reference 丢失 | 旧 `LegacyDocsRedirect` 会把 behavior 下的 topic slug 也误导到 events 域。 | 已按 topic / event 区分跳转：例如 `behavior-rule-syntax` 现在落到 `/docs/reference/behavior/behavior-rule-syntax`。 |
| 已完成 | 显式入口不足 | 行为页原本只露出 3 个 topic reference。 | 已补充 DSL 语法、CRUD 快速模式、最佳实践等显式入口。 |

## 后续只在以下情况下重拍/重生成图片

- 新文案与现有画面仍无法一一对应。
- 同一页面仍出现“同一状态被拆成多个步骤”的误导。
- 需要展示的关键对象不在当前截图可视范围内，只能靠错误框选硬解释。
- 同一篇文档里虽然文件不同，但底层状态重复到无法支撑不同步骤。
