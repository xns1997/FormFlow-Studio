# 「按任务学习」文档步骤配图整改 Todo

> 本文件只定义后续执行计划。执行时必须逐页完成、逐页确认，不得在一页尚未验收时批量处理后续页面。
>
> 导出版清单见：`docs/compose/plans/2026-08-02-task-learning-screenshot-audit-export.md`

## 目标

- 遍历“按任务学习”的 8 篇生命周期文档。
- 逐条校准步骤描述、真实产品截图和框选区域。
- 删除重复、无关或只起装饰作用的截图。
- 图片下方只强调用户当前要完成的步骤，不展示 Playwright、2×、高清截图等制作信息。
- 模板相关的通用操作集中到一篇独立文档，不在每个模板页面重复铺图。

## 当前执行策略

- 先处理 8 篇“按任务学习”主文档，再补模板相关参考页的联动检查。
- 「模板应用」任务页只保留简明流程和通用指南入口，不重复承载整套截图。
- 新增一篇独立文档《通用模板使用逻辑》，承接模板中心搜索、字段映射、预览、应用、质量检查和失败恢复。
- 单个模板参考页默认不配通用截图；只有存在专属配置、专属字段映射或独特结果预览时才单独补图。
- 每页验收完成前，不开始下一页的正式勾选。

## 通用验收规则

每篇文档必须按以下顺序处理：

- [x] 对照真实产品操作，确认步骤顺序和文案。
- [x] 同一篇文档的截图必须来自同一条连续操作链，前后数据状态、字段数量、记录范围和生成结果要能彼此对上。
- [x] 为确实需要视觉说明的步骤进入对应产品状态。
- [x] 使用 Playwright 截取 1600×1000、DPR 2 的真实界面。
- [x] 框选当前步骤真正需要点击、填写或检查的区域。
- [x] 检查图片、步骤描述和框选区域是否一一对应。
- [x] 检查连续步骤之间的项目、数据表、模板、字段数量和结果预览是否保持连贯；若中途切换数据状态，必须重拍整组截图。
- [x] 检查同页截图是否重复；重复且不能提供新信息时删除。
- [x] 检查桌面端与移动端布局、替代文本和图注。
- [x] 单页自动化检查和人工截图复核通过。
- [x] 记录该页验收结果，再开始下一页。

## 01「认识与创建」

- [x] 第 1 步：项目列表中的“新建项目”入口。
- [x] 第 2 步：创建向导中的空白项目/内置模板选择。
- [x] 第 3 步：项目名称和用途等基础信息。
- [x] 第 4 步：创建后的编辑器，核对项目名称和当前工作模式。
- [x] 确认四张图属于四个不同的产品状态。
- [x] 完成第 01 页验收并记录结果。

## 02「导入与建模」

- [x] 第 1 步：数据工作区的文件导入入口和已选文件状态。
- [x] 第 2 步：解析后的 Sheet、表头、样本值和字段类型。
- [x] 第 3 步：主键配置以及唯一性校验入口和结果。
- [x] 第 4 步：空值/重复值修复后的数据配置保存状态。
- [x] 确认截图没有用质量中心代替主键配置等错误映射。
- [x] 完成第 02 页验收并记录结果。

## 03「生成与设计表单」

- [x] 第 1 步：从目标数据表生成表单的真实入口。
- [x] 第 2 步：表单设计器中的显示名称和保存字段。
- [x] 第 3 步：控件属性面板中的必填、格式和数据绑定。
- [x] 第 4 步：预览/使用状态中完成一次有效填写。
- [x] 确认四步截图来自同一次生成链路，字段数量、字段名称和生成结果前后一致。
- [x] 确认四张图不是同一张表单设计器截图换框选。
- [x] 完成第 03 页验收并记录结果。

## 04「行为与流程」

- [x] 第 1 步：可视化规则中的字段联动配置。
- [x] 第 2 步：流程画布中的节点创建和端口连接。
- [x] 第 3 步：按钮或事件绑定目标流程的位置。
- [x] 第 4 步：规则 lint 与测试运行的真实结果。
- [x] 确认行为编辑、流程编排、事件绑定和验证结果各自对应。
- [x] 完成第 04 页验收并记录结果。

## 05「模板应用」与通用模板指南

模板文档不执行“每一步强制配图”。只有图片能解释独立操作差异时才保留。

### 新增独立文档「通用模板使用逻辑」

- [x] 新增独立文档及稳定路由，并从“模板应用”任务页和模板参考页链接过去。
- [x] 说明模板类型、适用范围以及使用前提。
- [x] 说明在模板中心搜索和选择模板的通用逻辑。
- [x] 说明数据表、主键、字段要求和原位诊断。
- [x] 说明字段映射、参数配置和生成内容预览。
- [x] 说明应用、质量检查、撤销/清理和失败恢复。
- [x] 只为“搜索与选择模板”“字段映射与预览”“应用后检查与失败恢复”等确实依赖界面的步骤配置截图。
- [x] 每张截图必须来自不同且对应的产品状态。

### 调整现有模板文档

- [x] “模板应用”任务页改为简明流程和通用指南入口，不重复整套截图。
- [x] 单个项目模板与操作模板页面默认不放通用步骤截图。
- [x] 只有存在专属配置、专属字段映射或独特结果预览时，才添加该模板自己的截图。
- [x] 删除所有只因关键词命中而自动添加的模板截图。
- [x] 完成模板任务页、新通用指南和模板参考页的联合验收。

## 06「测试与质量」

- [x] 第 1 步：自动测试样例中的覆盖率与样例总览。
- [x] 第 2 步：运行全部后，正常、空值与主键冲突等场景结果。
- [x] 第 3 步：质量页中的质量分数、趋势和问题分布。
- [x] 第 4 步：返回测试面板后确认最近运行时间与通过情况。
- [x] 禁止四步重复使用同一张自动测试总览图。
- [x] 完成第 06 页验收并记录结果。

## 07「使用与导出」

- [x] 第 1 步：使用页中的真实表单操作。
- [x] 第 2 步：数据页中的搜索、筛选和当前结果范围。
- [x] 第 3 步：导出结果入口与当前导出范围。
- [x] 第 4 步：导出成功提示与下载文件行数核对。
- [x] 确认“选择导出”和“核对结果”不是同一张界面。
- [x] 完成第 07 页验收并记录结果。

## 08「打包与发布」

- [x] 第 1 步：打开“发布检查”确认当前有 6 个自动测试阻断项。
- [x] 第 2 步：切到“自动测试样例”，核对失败用例与发布阻断项一一对应。
- [x] 第 3 步：打开设置页的“发布”分组，确认默认导出格式、输出文件名和写回策略。
- [x] 第 4 步：点击“保存”，记录当前交付策略已保存，后续只需先清掉自动测试阻断再发布。
- [x] 禁止四步重复使用同一张发布门禁截图。
- [x] 完成第 08 页验收并记录结果。

## 全量回归

- [x] 汇总每页的步骤数量、截图数量、资产路径和框选坐标。（见 `docs/compose/plans/2026-08-01-task-learning-screenshot-audit-report.md`）
- [x] 对任务配图计算文件哈希，检查无意义的重复图片。
- [x] 逐条检查图注和替代文本是否包含准确的步骤动作。
- [x] 确认无步骤文档没有通用顶部图。
- [x] 确认模板参考页没有批量重复的通用模板截图。
- [x] 逐页运行真实路由 DOM 检查，截图数量与批准的 Todo 一致。
- [x] 完成桌面端和移动端视觉复核。
- [x] 运行专项测试、UI 类型检查、完整测试和生产构建。
- [x] 完成代码审查后提交。

## 单页验收记录模板

```text
页面：
步骤文案：通过 / 需修改
截图状态：通过 / 需重拍
框选区域：通过 / 需调整
桌面端：通过 / 需调整
移动端：通过 / 需调整
自动化检查：
人工确认结论：
遗留问题：
```
