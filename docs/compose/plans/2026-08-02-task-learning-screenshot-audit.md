# 「按任务学习」文档步骤配图整改 Todo

> 基线日期：2026-08-02  
> 导出版清单：`docs/compose/plans/2026-08-02-task-learning-screenshot-audit-export.md`

## 本轮执行顺序

1. 先纠正任务步骤的文字描述，使其准确描述当前截图里的动作和检查点。
2. 再根据新文案检查每张图是否仍然成立；不成立则删图、并步或重拍。
3. 最后统一回归 DSL / reference 入口，确保文档能被点到、搜到、打开。

## 已完成的结构性修正

- 任务文档步骤文案已先按现有截图改写，不再使用“理想流程”覆盖“当前画面”。
- `测试与质量` 已从 4 步收敛为 3 步，不再重复展示同一张测试总览图。
- `使用与导出` 已改回当前真实画面对应的数据页筛选与导出链路。
- DSL / reference 路由已改为 canonical path 解析，不再生成不存在的 `/docs/:slug`。
- 行为页显式入口已补全到 DSL 语法、CRUD 快速模式和最佳实践。

## 当前验收口径

- 步骤描述必须能直接指向截图中的操作对象或检查结果。
- 同一篇文档内每个步骤必须属于同一 `scenarioId`，并带明确 `stateCheckpoint`。
- 同一篇文档不允许复用同一张截图资源。
- 如果两步实际上是同一状态，只保留一处，不再拆成伪步骤。

## 待持续关注

- 当前 8 篇任务文档已至少完成一轮“先定语意，再验图，再修图”的逐页复核。
- 若后续页面演进导致 `打包与发布` 第 2 步失败列表不再稳定对应发布阻断项，或 `使用与导出` / `导入与建模` 再次出现前缀匹配误导，则按导出版清单继续重拍或重生成图片。
