import { useMemo, useState } from 'react';
import { buildLineage, impactAnalysis } from '../services/data/lineage';
export function DataLineage({ project }: { project: unknown }) {
  const graph = useMemo(() => buildLineage(project), [project]); const [selected, setSelected] = useState<string>(); const impacted = new Set(selected ? impactAnalysis(graph, selected) : []);
  return <div className="lineage"><svg role="group" aria-label="数据血缘图" viewBox={`0 0 900 ${Math.max(300, graph.nodes.length * 42)}`}>{graph.edges.map((edge) => { const a = graph.nodes.findIndex((node) => node.id === edge.source); const b = graph.nodes.findIndex((node) => node.id === edge.target); return a < 0 || b < 0 ? null : <line key={edge.id} x1={180 + (a % 3) * 280} y1={30 + a * 38} x2={180 + (b % 3) * 280} y2={30 + b * 38} stroke="currentColor" opacity=".25"/>; })}{graph.nodes.map((node, index) => <g key={node.id} role="button" tabIndex={0} aria-pressed={selected === node.id} aria-label={`选择血缘节点 ${node.label}`} onClick={() => setSelected(node.id)} onKeyDown={(event) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); setSelected(node.id); } }} className={impacted.has(node.id) ? 'lineage-impacted' : ''}><rect x={40 + (index % 3) * 280} y={10 + index * 38} width="220" height="30" rx="6"/><text x={52 + (index % 3) * 280} y={30 + index * 38}>{node.label}</text></g>)}</svg></div>;
}
