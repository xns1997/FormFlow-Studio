import type React from 'react';

export const ios = {
  field: {
    width: '100%',
    height: '100%',
    minWidth: 0,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'stretch',
    gap: 6,
    padding: '4px',
    boxSizing: 'border-box',
    overflow: 'hidden',
  } satisfies React.CSSProperties,
  label: {
    flex: '0 0 auto',
    minWidth: 0,
    fontSize: 12,
    lineHeight: 1.25,
    fontWeight: 700,
    color: '#334155',
    letterSpacing: 0.2,
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
  } satisfies React.CSSProperties,
  control: {
    flex: '0 0 auto',
    minWidth: 0,
    minHeight: 40,
    width: '100%',
    maxWidth: '100%',
    height: 42,
    padding: '0 14px',
    border: '1px solid rgba(148,163,184,0.18)',
    borderRadius: 14,
    background: 'linear-gradient(180deg, rgba(255,255,255,0.98), rgba(248,250,252,0.92))',
    color: '#1c1c1e',
    fontSize: 14,
    lineHeight: 1.25,
    outline: 'none',
    boxSizing: 'border-box',
    boxShadow: '0 10px 22px rgba(15,23,42,0.05), inset 0 1px 0 rgba(255,255,255,0.85)',
    backdropFilter: 'blur(20px) saturate(180%)',
  } satisfies React.CSSProperties,
  fillControl: {
    flex: '1 1 auto',
    minWidth: 0,
    minHeight: 0,
    width: '100%',
    height: '100%',
    padding: '12px 14px',
    border: '1px solid rgba(148,163,184,0.18)',
    borderRadius: 16,
    background: 'linear-gradient(180deg, rgba(255,255,255,0.98), rgba(248,250,252,0.92))',
    color: '#1c1c1e',
    fontSize: 14,
    lineHeight: 1.35,
    outline: 'none',
    boxSizing: 'border-box',
    boxShadow: '0 10px 22px rgba(15,23,42,0.05), inset 0 1px 0 rgba(255,255,255,0.85)',
    backdropFilter: 'blur(20px) saturate(180%)',
  } satisfies React.CSSProperties,
  glass: {
    flex: '1 1 0',
    minWidth: 0,
    minHeight: 0,
    width: '100%',
    height: '100%',
    boxSizing: 'border-box',
    background: 'linear-gradient(180deg, rgba(255,255,255,0.98), rgba(248,250,252,0.94))',
    border: '1px solid rgba(148,163,184,0.14)',
    borderRadius: 18,
    overflow: 'hidden',
    boxShadow: '0 16px 32px rgba(15,23,42,0.06), inset 0 1px 0 rgba(255,255,255,0.85)',
    backdropFilter: 'blur(22px) saturate(180%)',
  } satisfies React.CSSProperties,
  naturalPanel: {
    flex: '0 0 auto',
    minWidth: 0,
    width: '100%',
    minHeight: 40,
    boxSizing: 'border-box',
    background: 'linear-gradient(180deg, rgba(255,255,255,0.98), rgba(248,250,252,0.94))',
    border: '1px solid rgba(148,163,184,0.14)',
    borderRadius: 16,
    overflow: 'hidden',
    boxShadow: '0 14px 28px rgba(15,23,42,0.06), inset 0 1px 0 rgba(255,255,255,0.85)',
    backdropFilter: 'blur(22px) saturate(180%)',
  } satisfies React.CSSProperties,
  row: {
    width: '100%',
    minWidth: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 8,
    padding: '10px 12px',
    boxSizing: 'border-box',
    borderBottom: '0.5px solid rgba(60,60,67,0.10)',
  } satisfies React.CSSProperties,
  muted: {
    minWidth: 0,
    flex: '1 1 auto',
    color: '#64748b',
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
  } satisfies React.CSSProperties,
};

export const requiredMark: React.CSSProperties = {
  color: '#ff3b30',
  marginLeft: 4,
  fontWeight: 700,
};

export const controlText = (overrides: React.CSSProperties = {}): React.CSSProperties => ({
  flex: overrides.flex,
  minWidth: 0,
  overflow: 'hidden',
  textOverflow: 'ellipsis',
  whiteSpace: 'nowrap',
  letterSpacing: 0,
  ...overrides,
});
